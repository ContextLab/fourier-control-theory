# Round 3 live verification (c65dfd2) — BLOCKING none. Minor:
1 tutorial §2.6 Try-this "shifts downward" only for f>0; f<0 shifts up (arg H = -arctan(f/fc)).
2 editor -2.5 -> -2 (should be -3), amp -0.5 -> 0 (should be 0.5, phase+π) — editor likely pre-clamps before store; jointPlot title raw "θ_k(t)" first glyph clipped left, "1.00" x-tick clipped right (1440 & 390); Control right card big empty column at 1440; hero ~55px dead padding at 1280.
3 390px spectrum hint lines overlap each other and stems, right end cut off.
4 Control arm trail doesn't reset on B/fc change (jagged hook ~1s).
5 Square Try-this B=1->3->7: only short trail on Control arm; show full band-limited path so corner sharpening visible.
6 dark hero title dark-green gradient on navy low contrast.
PASSED everything else.
